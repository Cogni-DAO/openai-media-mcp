id: only-openai-media
schema_version: "0.1"
blocking: false

evaluation-statement: Only allowed tool implementations are to OpenAI's media APIs.

# Code-aware capabilities (vendor-prefixed for non-breaking compatibility)
x_capabilities:
  - diff_summary      # Enable enhanced diff summary with file stats
  - file_patches      # Include actual patch content for analysis

success_criteria:
  metric: score
  threshold: 0.8